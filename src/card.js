import styled from 'styled-components';
import { Panel, PanelBlock, PanelHeading } from './bulma.js';

export const Card = styled(Panel)`
  flex: 1 400px;
`;

export const CardHeader = styled(PanelHeading)``;

export const CardBody = styled(PanelBlock)``;
